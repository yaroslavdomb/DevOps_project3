apiVersion: v1
kind: Service
metadata:
  name: jenkins-service
  namespace: jenkins
spec:
  clusterIP: None
  selector:
    app: jenkins-master
  ports:
    - protocol: TCP
      port: 50000
      name: inbound
      targetPort: 50000