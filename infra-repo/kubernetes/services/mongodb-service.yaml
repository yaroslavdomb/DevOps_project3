apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: application
spec:
  clusterIP: None
  selector:
    app: mongodb
  ports:
    - protocol: TCP
      port: 27018
      targetPort: 27018