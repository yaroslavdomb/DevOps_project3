apiVersion: v1
kind: Service
metadata:
  name: nexus-service
  namespace: nexus
  labels:
    app: nexus
spec:
  ports:
    - port: 8081
      targetPort: 8081
      name: web
    - port: 8083
      targetPort: 8083
      name: docker
  selector:
    app: nexus
  type: ClusterIP 